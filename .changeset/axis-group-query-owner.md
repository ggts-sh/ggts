---
"@ggts-sh/core": patch
---

Keep lazy axis-group indexing and member selection behind one internal query boundary. Preserve seed retention, panel isolation, rank ordering, and flipped-axis behavior.

Migration: none — the public CandidateStore API is unchanged.
