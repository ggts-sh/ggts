---
"@ggsvelte/svelte": minor
"@ggsvelte/skill": minor
---

Add `tooltipTotal` on `<Inspect>` so stacked charts can show a group Total without switching inspect `mode` to `"x"` or `"y"`.

Migration: none — additive
