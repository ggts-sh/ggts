{
  "name": "@ggts-sh/svelte",
  "version": "0.43.1",
  "description": "ggts: a layered grammar of graphics for Svelte 5 — ggplot2 semantics, JSON-serializable specs, agent-friendly validation, and hybrid SVG/canvas rendering. The ggts render CLI ships separately as @ggts-sh/cli.",
  "keywords": [
    "charts",
    "components",
    "dataviz",
    "ggplot2",
    "grammar-of-graphics",
    "interactive",
    "plot",
    "svelte",
    "svelte5",
    "visualization"
  ],
  "homepage": "https://ggts.sh/",
  "bugs": "https://github.com/ggts-sh/ggts/issues",
  "license": "MIT",
  "author": "Liam O'Dea",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ggts-sh/ggts.git",
    "directory": "packages/svelte"
  },
  "bin": {
    "ggts-codemod": "bin/ggts-codemod.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "svelte": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./data": {
      "types": "./dist/data/index.d.ts",
      "default": "./dist/data/index.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "svelte-package -i src/lib -o dist",
    "check": "svelte-check --fail-on-warnings",
    "test": "bun run test:browser && bun run test:ssr",
    "test:browser": "vitest run",
    "test:ssr": "vitest run --config vitest.ssr.config.ts",
    "test:coverage": "vitest run --coverage --project chromium && vitest run --coverage --config vitest.ssr.config.ts"
  },
  "dependencies": {
    "@ggts-sh/compose": "^0.43.1",
    "@ggts-sh/core": "^0.43.1",
    "@ggts-sh/spec": "^0.43.1"
  },
  "devDependencies": {
    "@sveltejs/package": "^2.5.8",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@testing-library/svelte-core": "^1.1.3",
    "@vitest/browser-playwright": "^4.1.11",
    "@vitest/coverage-v8": "^4.1.11",
    "axe-core": "^4.13.0",
    "playwright": "1.61.1",
    "svelte": "^5.57.0",
    "svelte-check": "^4.7.4",
    "typescript": "^6.0.3",
    "vite": "^8.2.0",
    "vitest": "^4.1.11"
  },
  "peerDependencies": {
    "svelte": "^5.33.1"
  },
  "engines": {
    "node": ">=22"
  }
}
